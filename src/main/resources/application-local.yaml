spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/trusta_market
    username: trusta_admin
    password: trusta_admin_pw
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

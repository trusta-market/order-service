spring:
  application:
    name: order-service

  profiles:
    active: local

  config:
    import: "optional:configserver:http://localhost:8888"

  cloud:
    config:
      label: develop
      fail-fast: false
